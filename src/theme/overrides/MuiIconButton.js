import palette from '../palette';

export default {
    root: {
        color: palette.icon,
        borderRadius: "6px",
        padding: 5,
        '&:hover': {
            backgroundColor: 'rgba(0, 0, 0, 0.03)'
        }
    }
};